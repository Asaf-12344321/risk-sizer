data.json
out/
__pycache__/
qa/node_modules/
qa/package-lock.json
RFC-*.md

# Design reference and handoff bundles. Not sensitive (verified free of account figures)
# but not needed by anyone cloning this repo. NOTE: untracked means unbacked-up — if these
# should survive a lost laptop, track them instead; `git add -f docs/` is all it takes.
docs/
